import { theme } from '@chakra-ui/core';

export default {
  ...theme
};

export const customColor = {
  action_icon_bg: { light: 'gray.200', dark: 'gray.600' },
  card_bg: { light: 'gray.50', dark: 'gray.700' }
};
